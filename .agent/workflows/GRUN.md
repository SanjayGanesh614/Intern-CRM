---
description: Start the development servers for backend and frontend
---

1. Open a terminal and run the backend server.
   Use the command: `npm run dev` inside the `backend` directory.

// turbo
2. Open a new terminal and run the frontend server.
   Use the command: `npm run dev` inside the `frontend` directory.
